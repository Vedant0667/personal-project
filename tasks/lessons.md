# Lessons

## Format: [date] | what went wrong | rule to prevent it

---

2026-06-12 | Diagnosing Montir's referral attribution gap (18 launch users connected to an existing user, only 1 `referrals` row), I asserted the single success "dylmil → sean_millimet was almost certainly manual code entry" — but Montir has NO manual-entry path; that success came through the automatic deep-link flow. I stated a mechanism as near-fact while explicitly unable to see the app client code. | Don't assert how an app behaves (a UI path, an input method, a code flow) when the relevant client code isn't in the repo you can see. Say "I can't confirm without the app code" and frame it as a hypothesis to test, not a near-certainty. Same family as the invented-URL lesson: never present unverified product behavior as fact.

2026-06-07 | The detect-correction Stop hook keeps firing on routine, non-correction requests ("change the period to an exclamation mark", "take away the apple"). Its reported "matched pattern" is a chunk of the injected CLAUDE.md text, not the user's message — so it's scanning the wrong content (likely the SessionStart-injected global instructions) and treating ordinary follow-ups as corrections. | The detector must scan ONLY the latest real user-turn message, never SessionStart/system-injected context (CLAUDE.md, lessons, hook output). When the hook fires on a clearly non-corrective request, don't fabricate a lesson to appease it — note the false positive and, if it recurs, fix the hook's input scoping instead of padding lessons.md.

2026-06-07 | User said "campuslife should show a phone too"; I read "too" as "in addition to the existing Apple mark" and rendered phone + apple, but they meant "it's a phone app like the others" and then had to tell me to remove the apple. | When an instruction word like "too/also/as well" is ambiguous between ADD and MATCH-THE-OTHERS, pick the reading that keeps the set consistent (here: one symbol per card like every sibling), or ask in one line — don't default to the additive reading that makes one item an outlier.

2026-06-07 | When asked for "small badges ... like an app store thing / a presenting icon," I first built icon+TEXT pill badges; user corrected to "i meant like symbols" (icon only). Then the bare icons sat optically high next to the large display serif ("looks a little off"). | "Badge/symbol" next to a name means a glyph, not a labeled pill — default to icon-only unless text is requested. When placing an icon beside large display type, align to the cap-height center (small downward optical nudge, ~8-10% of icon size), not the taller line-box center that `items-center` gives; verify with a zoomed screenshot before calling it done.

2026-06-07 | I jumped into brainstorming without creating/maintaining tasks/todo.md and tasks/lessons.md, and didn't update them as work progressed. User called it out twice. | Persist state continuously: mark a task `[~]` in tasks/todo.md BEFORE starting and `[x]` immediately after; append to tasks/lessons.md after EVERY correction — never leave state only in the conversation.

2026-06-07 | Hero/SEO copy I (and subagents) wrote read as corny AI slop: "builds real products", "I ship software and run a nonprofit: an App Store app, AI tooling, and X", colon-lists, superlatives. User rejected it repeatedly. | Write understated, specific, human copy. No colon-lists of accomplishments, no superlatives ("real products", "innovative", "passionate"), no marketing cadence. Prefer plain concrete facts; when in doubt, say less and let the work carry it. Read copy back and delete anything a bot would write.

2026-06-07 | First "redesign" pass only restyled the existing layout; user said it "didn't change it enough" and still felt like a plaything. | When the user asks for a redesign/overhaul, change decisively — palette, fonts, AND structure — not incrementally. A reskin is not an overhaul. Confirm the aesthetic direction up front for big visual changes so a large effort doesn't miss.

2026-06-07 | Newly installed .claude/hooks don't run in the session that installs them. | Claude Code reads settings.json only at launch — tell the user to restart CC to activate freshly-installed hooks; don't assume they're live mid-session.

2026-06-07 | Tailwind arbitrary classes (e.g. object-[center_38%]) sourced from data .ts files DO get generated. | Tailwind v4 scans all src/**.ts(x) raw text, so class-string literals in data files like projects.ts are detected — safe to drive object-position/etc. from data, as long as the value is a literal string in source (not concatenated).

2026-06-07 | User flagged things as "not working"/broken mid-task (hooks, montir modal crop). | Treat any "why isn't X working / it's broken" signal as a correction: stop, fix the root cause, AND immediately append a lessons.md entry — don't defer logging to end of session.

2026-06-07 | The detect-correction Stop hook couldn't recognize a written lesson: it checked `git diff tasks/lessons.md`, but tasks/ is UNTRACKED so the diff is always empty — it only avoided an infinite loop via the stop_hook_active guard. It also demanded a bracketed `[YYYY-MM-DD]` date while the file uses bracket-less dates, and scanned the last 8 user messages so stale "fix this" kept re-firing. | A hook that asks "did I edit file Y this turn?" must NOT assume Y is git-tracked — use file mtime (or content) as the primary signal and git diff only as a secondary. Keep detector format-strings consistent with the real file format, and scan only the latest user message.

2026-06-07 | I proactively added project links the user hadn't confirmed: a Stowr "stowr.app" link (user wanted stowr-landing.vercel.app) and a Canary "Live demo" link (no live demo exists — that URL belonged in achievements). | Don't invent or assume live/demo URLs for project cards. Only add a link the user explicitly provided or that is verified-deployed; when unsure, ask or leave it off.
